# 验收标准 Acceptance Criteria

## 静态站点

- [ ] 不运行 Node 服务也能访问生产输出；
- [ ] 首页、文章、程序和关于我可访问；
- [ ] 每篇文章和程序可直接刷新；
- [ ] 404 正常；
- [ ] 资源路径在目标环境正常。

## 做点啥呢

- [ ] 所有条目均为本人编写或贡献明确；
- [ ] 每项有状态、演示类型和限制；
- [ ] 静态演示不会伪装完整后端；
- [ ] 源码缺失时不显示空按钮；
- [ ] 演示失败不影响详情阅读；
- [ ] iframe 使用最小权限。

## 首页场景

- [ ] 0% 明显是陆地；
- [ ] 陆地 0%–30% 的中间进度中，引导人物的大部分行走路径不被 Hero、三张文章路牌或路牌柱遮挡；
- [ ] `≤980px` 的三张文章路牌可横向滚动和键盘聚焦，且不制造整页横向滚动；
- [ ] 50% 明显是深海；
- [ ] Programs 标题、三个档案终端和总入口按顺序可读，不发生标题/卡片或卡片/卡片持续重叠；
- [ ] 90% 明显是星空；
- [ ] 不看标题也能区分世界；
- [ ] 陆地到深海不是直接切色；
- [ ] M4.5 的陆海交接有三层浪花与泡沫遮挡，M5 不重复绘制海浪；
- [ ] 海洋到星空依靠颜色、光束与粒子形态平滑过渡；
- [ ] 海空过渡由同一固定 `MorphParticle` 池依次表现气泡、星点和少量流星，不清空后重建无关星点；
- [ ] 64%、66%、69%、72%、75.5%、78.5%、80% 和 82% 的形态变化连续，82%→64% 可恢复深海；
- [ ] 桌面/375px/Reduced Motion 的流星比例分别不超过 `5.5%`/`2.2%`/`0`；
- [ ] M5.5 固定流星全部朝右下，拖尾位于头部左上，原数量、位置和视差关系不变；
- [ ] M5.5 高速流星不读取滚动进度，只在星空中偶发运行，离开章节、切后台和卸载时完整清理；
- [ ] M5.5 行星具有核心、内亮、外扩散、环境散射和前后分层行星环；
- [ ] M5.5 三层浪使用透明生产 sprite，前/中/后轮廓、相位、速度和透明度有明显差异；横向泡沫带与水下气泡簇用途不混淆且无明显接缝；
- [ ] Canvas 背景世界和 DOM 在全部进度始终保持水平，不发生旋转；
- [ ] 向上滚动可完整倒放；
- [ ] 停止滚动时状态稳定。

## 移动端与无障碍

- [ ] 375px 无横向滚动；
- [ ] 按钮可触控且不依赖 hover；
- [ ] 粒子按视口和动效偏好降级；
- [ ] Skip link、可见焦点、键盘导航、图片 alt；
- [ ] Canvas aria-hidden；
- [x] 普通网址在无 URL 覆盖、无已保存偏好时直接进入完整动画，不要求添加 `?motion=full`；
- [x] fixed UI 提供可触控、可键盘聚焦、焦点可见且标签明确的完整/简化动画开关；
- [x] 用户主动选择在刷新、站内跳转和再次访问后保持；本地存储不可用时页面仍正常且控制台无未处理错误；
- [x] 模式优先级为有效 URL 参数 > 已保存选择 > 完整动画默认值，`?motion=full` 和 `?motion=reduce` 均不污染已保存偏好；
- [x] 系统 `prefers-reduced-motion: reduce` 可被识别并在完整模式下显示简化建议，一次操作即可切换；
- [x] 已保存简化模式在场景初始化前生效，不短暂播放完整视差、浪花或动态流星；
- [x] 简化动画保留三个可辨识世界与完整内容，关闭高速视差和动态流星并完成生命周期清理；
- [x] 运行中切换模式保持当前进度、章节高度、内容顺序和世界 0°，不产生重复时间线、RAF、timer 或监听器；
- [x] `?motion=full` 在任意系统偏好下恢复标准阶段高度，`?motion=reduce` 进入同一简化分支，两者均保持内容层无重叠；
- [x] 在 64%、72%、76%、79%、80%、82%、90% 分别执行 full→reduce 和 reduce→full 后，进度误差不超过 `±0.01`，并能继续到 100% 再回到 0%；
- [x] 任意模式到达页面物理底部时，`globalProgress >= 0.999`、`phase=space`，星空主内容与四项星座入口可见；不得停在 sea/transition；
- [x] 连续切换至少 5 轮后只有最后一次恢复事务生效，没有旧 RAF/refresh 把页面拉回错误坐标；
- [x] 桌面完整与简化模式均常显四行“发光菱形星 + 标题”入口，内容为前两篇文章与前两个 Program，文字不依赖 hover；
- [x] 四项星座入口在桌面和 375px 均可点击、可 Tab 聚焦，焦点可见，目标路由正确，且不遮挡信号站、动效开关、阶段导航或页脚；
- [ ] 不自动播放声音。

## M6.1 深海 Programs 左右分栏与标题渐隐

- [ ] 1280×720 和 1920×1080 的桌面完整动画中，“沉在海里的程序档案”进入 Programs 阅读窗口后停驻在 section 左上安全区；
- [ ] 三张 Programs 卡片均位于标题右侧，并继续保持当前完整纵向排列、现有间距和自然出场；
- [ ] 单张卡片没有新增 sticky、fixed、独立步骤容器或接棒式显隐，三张卡片始终属于同一个普通文档流；
- [ ] 最后一张卡片划过前标题已经开始变暗，在卡片离开主要阅读区时标题透明度到达 `0`，没有突然消失；
- [ ] 标题渐暗只改变颜色、透明度和最多 `12px` 的轻微上移，不旋转、不闪烁、不让卡片同步变暗；
- [x] 在 43%、46%、51%、56%、59%、61%、64% 的标题/卡片、卡片/卡片和标题/fixed UI 矩形相交均为 0；当前主阅读卡片不被 fixed UI 遮挡，普通流离场片段只从浮动导航下方经过且不覆盖导航交互；
- [ ] 从 61% 向上滚回 43% 时标题颜色和透明度连续恢复，卡片保持普通文档流，不跳卡、不重建内容、不锁定滚动；
- [ ] 375×812 下标题在上、三张卡片在下，保持普通文档流、明确间距和零页面级横向溢出；
- [ ] 内容宽度不足与简化动画均采用同一单列降级，不恢复旧版 sticky 堆栈；
- [ ] 完整/简化切换后当前进度误差不超过 `±0.01`，并可继续到 100% 后回到 0%；
- [ ] DOM、屏幕阅读器和 Tab 顺序均为标题 → 卡片 01 → 卡片 02 → 卡片 03 → 总入口；
- [ ] Program 标题和操作链接可点击、可聚焦、目标正确，Canvas fallback 下内容完整；
- [ ] M4、M4.5、M5、M5.5、M6、Program 内容模型、路由和 15 个静态 HTML 无回归。

### M8-11 陆海卡片组出入场时机修正

- [x] 桌面完整动画在 28.5% 前保持文章路牌原 `28vh`/短视口 `30vh` 停靠坐标，28.5% 后开始向上离场，36.8% 时完成最大 `56vh` 上移；
- [x] Programs 卡片静态 padding 保持原 `--program-card-offset`，桌面完整动画叠加恒定 `54vh` 进入偏移；滚动过程中不增长、不释放、不产生方向反转；
- [x] “查看全部文章”和“打开全部程序”分别与对应卡片组同步，不出现脱节、重叠或拦截 fixed UI；
- [x] 正向与反向经过 28.5%、31.5%、34.2%、36.8%、43%、50%、59%、64%、66% 时位置可逆且无状态漂移；Programs 在连续向下滚动中每次采样的 `top` 必须单调减小；
- [x] 实现仍只有一个 ScrollTrigger；未修改章节高度、M4.5 浪花、M5 边界、小章鱼路径、卡片内容、尺寸、间距和顺序；
- [x] 375px、Reduced Motion 与 Canvas fallback 保持原文档流、三卡可见、链接可用且无横向溢出；
- [x] 本地开发预览控制台无新增 warning/error，Astro Check、ESLint、测试和生产构建通过。

## M6.2 跨世界小章鱼主题角色

- [x] `octopus1.png`、`octopus2.png`、`octopus3.png` 已处理为透明、裁切、基线统一的生产素材，运行时不直接读取 `assets/reference/`；
- [x] 普通小章鱼替换现有陆地小人，保留 `start: 0.13`、`travel: 0.46` 的行程基线和轻微待机浮动；
- [x] 普通小章鱼先向上跃起，再落入现有三层浪花，并在泡沫遮挡内切换为潜水形态；
- [x] `38%→30%` 反向滚动时，潜水形态经相同浪花遮挡恢复为普通形态，没有闪现或直接换图；
- [x] 潜水小章鱼在 `38%–62%` 从屏幕右上逐渐移动到左下，并叠加不改变主路径的轻微浮动；
- [x] 潜水小章鱼在 `62%–66%` 向下潜出，反向滚动时从屏幕下方升起；
- [x] 宇航员小章鱼只从屏幕下方升入星空，稳定后执行独立于滚动坐标的 DVD 式边界反弹；
- [x] 从 `90%` 向上回到约 `81.5%` 时，宇航员仍继续反弹，同时逐渐变暗、降低透明度；
- [x] 宇航员尚未完全消失便重新向下时，在同一位置和速度上恢复正常颜色与透明度，不重新入场、不创建重复 RAF；
- [x] 宇航员完全消失后清理漂浮循环；之后重新进入星空时从屏幕下方重新升入，并且只有一个活动实例；
- [x] 宇航员位于星球与信号站装饰前、文字卡片和交互内容后，不拦截指针、触控或键盘；
- [x] 1280×720、1920×1080 和 375×812 中角色不产生页面级横向溢出，不持续遮挡导航、阶段进度、动效开关或主要按钮；
- [x] 简化动画关闭持续浮动与 DVD 反弹，但保留三种角色和必要的形态切换；
- [x] Canvas fallback 下核心内容和交互完整，角色降级为当前章节的静态生产形态；
- [x] 页面隐藏/恢复、resize、模式切换、重复进入星空和卸载后没有重复 RAF、timer 或监听器；
- [x] M3～M6.1、五段全局进度、M4.5 浪花、M5/M5.5、世界 `0°`、Programs、路由和 15 个静态 HTML 无回归。

M6.2 的逐项代码、命令和浏览器证据记录在 `tasks.md` 与 `docs/engineering/adjustment_record.md` 第 11 节。

## M6.3 普通内容页纵向滚动

- [x] `/articles/`、文章详情、`/programs/`、Program 详情、`/about/` 和 404 均使用浏览器原生纵向滚动；
- [x] 内容超过一屏时可到达最后一项、详情末段和页脚，不被 `100vh` 或 `overflow-y: hidden` 裁切；
- [x] 桌面滚轮/触控板、PageDown/PageUp、Home/End 和 Tab 焦点推进有效；
- [x] 375×812 的触摸滚动有效且页面级横向溢出为 0；
- [x] 移动菜单仅在打开时锁定滚动，关闭、Escape、选择链接、resize 和卸载后恢复；
- [x] 详情页直接刷新、浏览器前进后退和站内跳转后仍可滚动；
- [x] 首页 ScrollTrigger、五段进度、动效模式、Canvas 和章节高度没有变化；
- [x] 控制台、自动化、生产构建与静态路由验证通过。

## M7 真实 Programs 展示系统

- [x] Program 详情页在标题、状态和简介之后立即显示“打开网页版”，位于长篇正文之前；
- [x] 桌面使用左文右媒体的普通文档流双栏，无 sticky、pin、重叠或嵌套主滚动；
- [x] 375px 顺序为简介 → 打开网页版 → 视频 → 小程序码 → 详细说明；
- [x] 同一 Program 可以同时表达真实网页版、演示视频和微信小程序入口；
- [x] 外链打开真实地址，使用新标签页安全属性，缺失地址时不显示空按钮；
- [x] 9:16 视频不自动播放、不自动播放声音，具有 controls、playsinline、poster/说明和诚实失败状态；
- [x] 小程序码由项目所有者提供，图像清晰、有 alt 和“微信扫码打开小程序”说明；实际微信扫码能力仍需发布后由项目所有者在微信真机复核；
- [x] Program 必备的本人贡献、核心功能、技术方案、限制与隐私内容仍完整；
- [x] “拉了么”的技术栈、贡献、限制、隐私、外部服务、小程序码和视频均来自项目所有者真实资料，不推测；Android 与源码入口按确认暂不公开；
- [x] Tidy Desk、Signal Garden 按项目所有者决定以 `prototype` 原型档案公开，明确演示、限制和隐私边界，未增加未经确认的在线或后端能力；
- [x] 大视频不进入首页包，外部程序的后端不成为主站运行时依赖；
- [x] Programs 搜索、筛选、SEO、Sitemap、`/projects` 兼容和详情静态刷新通过；
- [x] 桌面、375px、键盘、控制台、Astro Check、ESLint、测试和生产构建通过。

2026-08-09 对账：真实“拉了么”资料和三项生产媒体已接入；H.264、尺寸、静态输出、媒体隔离、17 个路由和质量门禁已验证。M7 完成；发布后仍需项目所有者用微信真机确认线上小程序码扫码结果及视觉观感。

## 质量

### M8-12 Programs 雷达扫描目标

- [x] `/programs/` 雷达 DOM 中恰有四个可循环目标，类型分别为小鱼、`</>`、`{ }` 和 `>_`；
- [x] 四种目标使用与雷达外圈相同的前景色；小鱼为扁菱形轮廓鱼身、较小三角轮廓鱼尾和较大单点眼，不包含实心鱼身、圆角或弧线，也不出现双扫描棒或难以辨认的重叠；
- [x] 每个目标只在扫描棒越过自身方位角后点亮，并在随机 `1.1–2.1s` 内连续渐隐至完全熄灭；
- [x] 目标完全熄灭后重新随机定位；页面不滚动并持续停留至少一个完整扫描周期时，能观察到位置变化；
- [x] 任意时刻仍只有四个目标节点和一个 RAF 循环；页面隐藏、pagehide、pageshow 与组件重建不会叠加循环或监听器；
- [x] Reduced Motion 下扫描、命中、渐隐与重新定位全部保留；375px 下四个目标仍在雷达圆内且页面无横向溢出；
- [x] 雷达保持 `aria-hidden`、不可点击、不可聚焦，不影响 Programs 搜索、筛选、卡片和导航；
- [x] Astro Check、ESLint、雷达自动测试、生产构建与浏览器控制台验证通过。

- [ ] 类型、Lint、单元、集成和 E2E 通过；
- [ ] 生产构建通过；
- [ ] 控制台无未处理错误；
- [ ] 页面切换后无重复时间线；
- [ ] 无明显内存持续增长；
- [ ] 文档同步完成。
